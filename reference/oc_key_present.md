# Is an OpenCage API key present?

Checks whether a potential OpenCage API key, i.e. a 32 character long,
alphanumeric string, is stored in the environment variable
`OPENCAGE_KEY`.

## Usage

``` r
oc_key_present()
```

## Value

A single logical value, `TRUE` or `FALSE`.
